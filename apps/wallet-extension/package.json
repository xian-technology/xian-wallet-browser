{
  "name": "xian-wallet-extension",
  "version": "0.3.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "test:browser": "npm run build && playwright test -c ./playwright.config.ts",
    "test:visual": "npm run build && playwright test -c ./playwright.config.ts ./tests/visual.spec.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@xian-tech/provider": "0.1.6",
    "@xian-tech/wallet-core": "0.3.2",
    "uqr": "^0.1.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "esbuild": "^0.25.4"
  }
}
