# Backlog

This is the stable backlog entrypoint for `xian-wallet-browser`.

Current themes:

- transaction history and portfolio UX
- verified official network presets once Xian public networks exist
- publishable browser-wallet packaging and release process
- stronger account management and multi-account UX
- extension-to-hosted-wallet shared flows if a browser web wallet is added
